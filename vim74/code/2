#include<cstdlib>
#include<cstdio>

using namespace std;

int n;
int num[300001];
long long ans;

int main()
{
	scanf("%d",&n);
	for (int i = 1; i <= n; ++i)
		scanf("%d",&num[i]);
	sort(num+1,num+n+1);
	for (int i = 1; i <= n; i++)
		ans += num[i];
	for (int i = n; i >= 1; --i)

	return 0;
}
