#include<bits/stdc++.h>

using namespace std;

double x1,x2,y1,y2;

int main()
{
	while (~scanf("%lf%lf%lf%lf", &x1, &y1, &x2, &y2))
	{
		printf("%.2lf\n",sqrt(pow((x1-x2),2)+pow((y1-y2),2))); 
	}
	return 0;
}
